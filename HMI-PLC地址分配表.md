# 腰康复仪 PLC ↔ HMI 地址分配表（D 寄存器）

> 依据《腰康复仪 PLC 重构需求书 —— 设备控制与 HMI 交互》（2026-08-31）整理。
> 平台：汇川 H5U A8 PLC（Modbus TCP 服务端）+ 3× M900 伺服（CiA402 Motion Control，EtherCAT 总线）+ 1 路电推杆。
> HMI 通过 Modbus TCP 读写 **D 寄存器区**。

---

## 0. 使用约定（务必先读）

1. **权威契约**：精确到每个变量的成员名 / 数据类型 / 嵌套层次，以旧工程 InoProShop 变量表导出 **`old/结构体/Stru_HMI_new.csv`**（UTF-16 原件，`Stru_HMI_new_utf8.csv` 为 UTF-8 转码副本）为准。新工程按该 CSV 逐字段定义线上结构体 `ST_W_HMI` 并整体映射 `AT %MW200`（D200），由编译器自动完成 BOOL 按位打包（16 位/字）、REAL/DINT 双字对齐——**PLC 侧结构体布局即与旧工程/HMI 完全一致，无需手工核算偏移，亦不得擅改字段名、类型、顺序**；确需变更须双方同步。本表按窗口范围整理，块内字段细节与 CSV 冲突时，**以 CSV 为准**。已核实总长度 nBitLen=11856 bit = 741 字（D200–D940），与各窗口求和一致。
2. **字宽**：`DINT` / `REAL` 占 **2 个 D 字**，预留时成对留；结构体中由编译器按双字边界自动对齐。
3. **脉冲命令**：HMI 置位 → PLC **上升沿检测**消费后自行清零。同一命令字内含 PLC 反馈位时，**只清目标命令位、保留反馈位**。
4. **电平命令**：按住有效（点动 Jog、维护授权、无负载确认、参数重校验），松开撤销；离开任务状态时 PLC 必须清残留电平。
5. **参数块**：HMI 先写参数区，再写 ConfigCmd 命令位；PLC 在同一命令字回写 `bSuccess / bError / iParamErrorCode`，不得主动改写参数区其余内容。应用失败不得破坏运行区。
6. **D 区仅用于 HMI 交互**；物理急停 / 复位按钮 / 脚踏 / 原点传感器为硬接线 I，电推杆 `Q_Pusher_Up / Q_Pusher_Down` 为 Q 输出，伺服走 EtherCAT 总线（CiA402，MC 指令），均不占 D 区。
7. **契约外空隙**：D1–D199、D941–D999、D1005–D1009、D1013+ 可作内部中间变量，但不得让 HMI 窗口跨越。（D660–D668 为 TailTilt.Motion 运动参数区，非空隙。）

---

## 1. 寄存器窗口总览

| 地址窗口 | 归属 | 方向 | 内容 |
|---|---|---|---|
| D200–D201 | Condition.Cmd | HMI→PLC | 调理命令位（S1/S2/S3、停止、脚踏、复位、暂停/恢复、跳过成角、Ppad1-7） |
| D202–D444 | Condition.Param | 双向 | 三轴往复参数块：SeatTwist D202–282 / LegAngle D283–363 / ChestImpact D364–444（每轴 16 段位置 + 16 段速度 + 模式字） |
| D445–D458 | Condition.Param | 双向 | 全局调理参数（成角角度、尾板初始角、批次数/间隔、超时、全局循环、信号模式） |
| D459–D478 | Condition.Status | PLC→HMI | 调理状态（阶段/步骤/循环、批次进度、错误码、17 个状态位） |
| D479–D480 | Condition.ParamCmd | 双向 | 调理参数操作（加载/应用/保存/恢复默认 + 成功/失败反馈） |
| D481–D489 | Sys.Cmd | HMI→PLC | 系统命令（工作模式、用户角色、权限码、15+ 命令位） |
| D490–D508 | Sys.Status | PLC→HMI | 系统状态（iState、iPhase、错误、健康、约 22 汇总位） |
| D509–D566 | Sys.ErrorQueue | PLC→HMI | 错误环形队列（约 8–9 条，每条：级别/码/来源/子码/附加值） |
| D567–D577 | Sys.Runtime | PLC→HMI | 运行统计（老化循环、总循环、运行时长、当前步骤） |
| D578–D584 | Sys.Error | PLC→HMI | 当前活动错误（级别/码/来源/子码/附加值 + 活动/致命/需复位位） |
| D585–D589 | Sys.Warning | PLC→HMI | 当前警告（独立通道） |
| D590–D605 | TailTilt.Cmd/Status/ConfigCmd | 双向 | 电推杆命令(D590)、状态(D592–603)、配置操作(D604–605) |
| D606–D659 | TailTilt.Config/Motion | 双向 | 电推杆配置（限位/标定/AD 拟合/堵转）与运动参数 |
| D669–D740 | ChestImpact（胸轴）块 | 双向 | 胸轴状态/命令/参数（同构块，72 字） |
| D741–D812 | SeatTwist（臀盘轴）块 | 双向 | 臀盘轴（同胸轴结构，72 字） |
| D813–D884 | LegAngle（臀腿轴）块 | 双向 | 臀腿轴（同胸轴结构，72 字） |
| D885–D935 | HomingParams | 双向 | 回零参数块（速度/接近区/迟滞/超时/模式；**D886 为操作命令字**） |
| D936–D940 | TailTiltAdFeedback | PLC→HMI | AD 拟合角度、AD 原始值、有效位 |
| D1000–D1004 | DeviceInfo（Stru_HMI_DeviceInfo） | PLC→HMI | 设备信息只读镜像：diDeviceNoRaw DINT（D1000–1001）+ iMacWord0/1/2 INT（D1002–1004，= _Ethernet.MACAddress[0..2]） |
| D1010–D1013 | DeviceIdentityWrite（Stru_HMI_DeviceIdentityWrite） | 双向 | 设备身份维护写入：Cmd（iErrorCode D1010 + bLoad/bApply/bSuccess/bError/bPad0/bPad1 打包 D1011）+ diDeviceNoRaw DINT（D1012–1013） |

---

## 2. 系统命令区（Sys.Cmd，D481–D489，HMI→PLC）

### 2.1 数据字

| 变量 | 含义 |
|---|---|
| iWorkMode | 工作模式：1=调理 2=治疗（复用 1 流程）3=老化测试 |
| iResetMode | 复位模式 |
| iHomingMode | 回零模式（预留） |
| iUserRole | 用户角色：0=操作员 1=工程师 |
| iAuthCode | 权限校验码：HMI 登录写 `16#5A`、退出写 `0`（防误写） |

### 2.2 命令位（INPUT_ADAPTER 边沿锁存 + 状态门控消费）

| 位 | 命令 | 类型 | 门控 / 语义 |
|---|---|---|---|
| D488.0 | 服务模式请求 | 脉冲 | 需工程师角色 |
| D488.1 | 回零启动 | 脉冲 | STANDBY / MANUAL 有效 |
| D488.2 | 系统级停止 | 脉冲 | 无条件 |
| D488.3 | 暂停 | 脉冲 | 运行/待机/手动/校准有效（透传调理） |
| D488.4 | 恢复 | 脉冲 | 同上 |
| D488.5 | 生产/调理流程启动 | 脉冲 | → AUTORUN |
| D488.6 | 校准请求 | 脉冲 | 仅 STANDBY / MANUAL |
| D488.8 | 错误确认 | 脉冲 | 清除/确认系统错误 |
| D488.9 | **软件急停** | 脉冲 | **无条件穿透，最高优先级** |
| D488.10 | 初始化请求 | 脉冲 | → INIT |
| D488.11 | 维护建零请求 | 脉冲 | 6 重门控（见 §8） |
| D488.12 | 自检安全确认 | 脉冲 | SELFTEST 内有效 |
| D488.13 | 跳过动态自检 | 脉冲 | SELFTEST 内有效 |
| D488.14 | 全轴断电 | 脉冲 | 仅 STANDBY / MANUAL |
| D488.15 | 全轴上电 | 脉冲 | 仅 STANDBY / MANUAL；PLC 分时上电 |
| D489.0 | 参数重校验 | **电平** | — |
| D489.1 | 维护授权确认 | **电平** | 建零门禁条件 |
| D489.2 | 无负载确认 | **电平** | 建零门禁条件 |
| D489.3 | 待机态开始动态自检 | 脉冲 | → SELFTEST |
| D489.4 | 服务完成 | 脉冲 | → READY_CHECK |
| D489.5 | 手动重新资格检查 | 脉冲 | → READY_CHECK |
| D489.6 | 快速启动 | 脉冲 | 需 bSkipSelfTestAllowed（操作员资格） |
| D489.7 | 强制跳过自检 | 脉冲 | 需 bForceSkipSelfTestAllowed（管理员资格） |
| D489.8 | 清除建零 Retain | 脉冲 | — |
| D489.9 | 冲击轴扭矩基线学习 | 脉冲 | 力估算基线标定 |

---

## 3. 调理流程命令区（Condition.Cmd，D200–D201，HMI→PLC）

| 位 | 命令 | 类型 | 消费点 / 语义 |
|---|---|---|---|
| D200.0 | S1 启动流程 | 脉冲 | FBI_Cond Step100→105 |
| D200.1 | S2 开始成角 | 脉冲 | Step108 人员准备确认 |
| D200.2 | S3 确认信号 | 脉冲 | Step120 开始运动确认 |
| D200.3 | 停止 | 脉冲 | 任意时刻 |
| D200.4 | 脚踏信号（软） | 电平/脉冲 | 等同物理脚踏 |
| D200.5 | 复位错误 | 脉冲 | — |
| D200.6 | 暂停 | 脉冲 | 运动保持、计时暂停 |
| D200.7 | 恢复 | 脉冲 | 同上 |
| D200.8 | 跳过成角 | 脉冲 | Step108 直接跳到确认步骤 |
| D201.0–6 | Ppad[0..6] | — | 预留垫片位 |

---

## 4. 三轴同构块（ChestImpact / SeatTwist / LegAngle）

块基址与步距：

| 轴 | 变量名 | 业务名称 | 块基址 | 块结束 | 命令字 |
|---|---|---|---|---|---|
| Chest | Axis_胸背 | 胸板冲击轴 | **D669** | D740 | **D702** |
| Seat | Axis_臀盘 | 臀盘扭角轴 | **D741** | D812 | **D774** |
| Leg | Axis_臀腿 | 臀腿角度轴 | **D813** | D884 | **D846** |

块内结构（每块 72 字，三轴完全同构，建议同一结构体映射）：

```
Status.Servo   26 字   伺服层状态
Status.Force   12 字   力估算层状态
Status.Business 11 字  业务层状态
Cmd             1 字   轴命令字（见下）
Motion          8 字   运动参数
Config         10 字   轴配置（Retain）
ConfigCmd       2 字   配置操作命令 + 反馈
```

### 4.1 轴命令字位定义（D702 / D774 / D846）

| 位 | 命令 | 类型 |
|---|---|---|
| 0 | 使能（触发伺服上电，POWER 仲裁分时上电） | **电平** |
| 1 | 复位（清轴错误） | 脉冲 |
| 2 | 原点标定（MC_Home，仅工厂建零） | 脉冲 |
| 3 | 停止（减速停车） | 脉冲 |
| 4 | 绝对定位 → rTargetPos | 脉冲 |
| 5 | 相对定位 ±rTargetPos | 脉冲 |
| 6 | 正点动 | **电平按住** |
| 7 | 负点动 | **电平按住** |
| 8 | 自动运动 | 内部流程位 |
| 9 | 缓冲运动 | 内部流程位 |
| 10 | 请求断使能 | **电平** |

### 4.2 Motion 块配套字

目标位置、目标速度、点动速度、加速度、减速度、初始位置、超时。

### 4.3 轴状态反馈（每轴约 49 字）

- **伺服层**：实际位置 / 速度 / 扭矩、轴错误码（9xxx）、驱动器故障码（Er.xxx）、定位运动用时、16 个状态位（使能/就绪/运动中/完成/静止/到位/定位运动/运动完成/建零完成/建零中/原点信号/正限位/负限位/错误/停止中/编码器故障）。
- **力估算层**（三轴都有，主要用于冲击轴）：正/负扭矩侧静止基线、净扭矩、未滤波/低通牵引力估算(N)、超力持续时间、力有效/基线已学习/超力实时/超力锁存。
- **业务层**：当前运动步骤、等待用时、运动用时、已标定/错误/警告/错误停止中/在初始位。

> ✅ **块内偏移已由 CSV 核实**：需求书 §3.2 表格中胸轴状态写作「D669–D717」、命令字 D702 与 26+12+11+1+8+10+2 结构推算的偏移矛盾，已按 `Stru_HMI_new.csv` 中 `ChestImpact / SeatTwist / LegAngle` 三块同构结构体逐字段核实解决：块基址 D669 / D741 / D813，步距 72 字，块内字段顺序、类型与打包由编译器按 CSV 结构体定义自动落位，无需手工核算。三轴块与 Condition.Param 往复参数块（SeatTwist/LegAngle/ChestImpact 顺序）为不同结构体，注意轴序约定：往复参数块线上顺序为 Seat→Leg→Chest，三轴块顺序为 Chest→Seat→Leg。

---

## 5. 电推杆区（TailTilt）

| 地址 | 内容 |
|---|---|
| D590 | 电推杆命令字 |
| D592–D603 | 电推杆状态 |
| D604–D605 | 配置操作命令（ConfigCmd）+ 反馈 |
| D606–D659 | 电推杆配置（Retain）与运动参数 |
| D936–D940 | AD 反馈：拟合角度、AD 原始值、有效位 |

- **命令字 D590**：使能、停止、复位、执行标定、自动定位、回零、正/负点动（上行/下行）、手动清零、重置故障。
- **Motion**：目标角度、手动 Preset 角度、限时 Jog。
- **状态 D592–D603**：当前角度、当前脉冲、最近 Preset 目标角、错误码、重试计数；已校准/已回零/回零中/原点信号有效/参考有效/传感器健康/卡死 ON/卡死 OFF/一致性通过/正反转输出/忙/完成/故障/警告/堵转/超时/正限位/负限位/在初始位/传感器已检验/自动定位锁存与门禁。
- **双参考模式** `iReferenceMode`：0 = X10 脉冲 + 原点传感器 HC；1 = AD 角位移编码器拟合角度（三次曲线系数 K0–K3 在配置中）。
- 配置项：角度限位/容差/初始位/最小行程/原点位置、脉冲当量、AD 归一化偏移与比例、AD 三次拟合系数 K0–K3、AD 跳变保护、上/下行惯性补偿、堵转电流/速度阈值、堵转确认时间、操作超时、换向缓冲、停车确认、AD 有效上下限与采样周期、最大重试、参考模式选择、方向取反。

---

## 6. 系统状态区（Sys.Status，D490–D508，PLC→HMI）

- **数值**：电推杆开机偏移量、本次开机运行时长、资格检查结果位掩码 `diReadyCheckResult`、`iState`、`iPhase`、初始化错误码、当前错误码/子码/级别/来源、健康等级（0 正常 / 1 建议校准 / 2 必须校准 / 3 故障）、当前活跃任务阶段与阻塞原因 `iBlockReason`、工作模式。
- **布尔（约 22 位）**：已初始化、Retain 已加载、自动流程活跃、全轴使能、分时上电中 bPoweringUp、存在需复位错误、开机原点传感器态、跳过自检标志、全轴参考有效、全轴在初始位、全轴空闲、任一轴限位、系统就绪 bReady、安全可启动 bSafeToStart、忙碌、参数错误、绝对位置可信、敏感运行（紧急回零/脱限位窗口）、急停中、自检完成、允许快速跳过、允许强制跳过。

### 6.1 系统状态码 iState（MODE_MANAGER 唯一写入）

| 值 | 状态 | 含义 | 可退往 |
|---|---|---|---|
| 0 | INIT | 上电初始化：Retain 加载、伺服探测、参数校验 | READY_CHECK / SELFTEST |
| 3 | READY_CHECK | 资格检查（零点/参数/硬件/健康/位置/参考 六项位掩码） | STANDBY / SELFTEST |
| 5 | SELFTEST | 动态自检 | STANDBY / SOFT_ESTOP |
| 10 | STANDBY | 待机，安全启动点 | 各工作状态 |
| 15 | SERVICE_REQUIRED | 检查不通过，要求进入服务模式 | SERVICE |
| 20 | AUTORUN | 自动调理 / 老化 | STANDBY / STOPPING |
| 30 | SERVICE | 服务/手动模式（点动、定位，工程师权限） | STANDBY |
| 40 | HOMING | 普通回零（三轴 MoveAbs，非 MC_Home） | STANDBY / RESET |
| 45 | CALIBRATE | 校准（电推杆标定等） | STANDBY |
| 50 | RESET | 系统复位（不含隐式回零） | STANDBY / READY_CHECK |
| 60 | STOPPING | 安全停止中 | STANDBY / SOFT_ESTOP |
| 110 | SOFT_ESTOP | 软急停（减速停车，含紧急 Jog 回零子流程） | RESET |
| 120 | HARD_ESTOP | 硬急停（使能直接断开） | RESET |
| 998 | PARAM_LIMITED | 参数错误受限模式（只允许修参数） | STANDBY |

---

## 7. 错误 / 警告 / 统计区

| 地址 | 内容 |
|---|---|
| D578–D584 | 当前活动错误：级别(0 Info/1 Warning/2 Critical)、错误码、来源模块码、子码(轴 9xxx / 驱动器 Er.xxx)、附加值×2、活动/致命/已升级/需手动复位位 |
| D585–D589 | 当前警告（独立通道，不升级停机，HMI 顶部提示） |
| D509–D566 | 错误环形队列（约 8–9 条历史，每条同构：级别/码/来源/子码/附加值） |
| D567–D577 | 运行统计：老化循环计数、总循环数、运行时长、当前步骤、当前循环、批量完成位 |

### 7.1 错误码分段

| 段 | 含义 |
|---|---|
| 1xxx | 系统 / 急停 |
| 11xx | 初始化 / 电源 |
| 12xx | 紧急回零 / 防夹 / 跟随误差 |
| 13xx | 自检（如 1303 校准超时、1304 试动超时） |
| 2xxx | 三轴 |
| 3xxx | 电推杆（3021/3022 传感器卡死、3023 一致性、3024 参考无效、3101–3107 校准步骤） |
| 4xxx | 回零 |
| 5xxx | 参数 / 设备身份（5005/5006 身份非法/锁定） |
| 7xxx | 调理启动前检查 |
| 9xxx | PLC 轴错误（子码） |
| Er.xxx | 伺服驱动器故障码（子码，如 Er.101/201/234 编码器故障） |

---

## 8. 调理状态区（Condition.Status，D459–D478，PLC→HMI）

- 实际运行时间、批次进度（当前/目标/间隔剩余）、阶段、当前步骤、当前/目标循环、调理错误三件套（码/级别/参数错误码+轴+字段）。
- **阶段码**：0 空闲 / 1 预备 / 2 运行 / 3 成角 / 7、9 完成。
- **步骤码（经 D468 暴露）**：105 启动前检查 → 108 等待人员准备 → 110 成角定位 → 120 等待开始确认 → 130 三轴联动往复 → 140 安全降落 → 800 完成；任一步出错 → 900 错误处理。
- **17 个状态位**：进行中、已完成、暂停、错误、任意动作中、等待成角、回零中、成角完成、等待脚踏、往复中、回零完成、手动停止、紧急中止、参数校验通过、参数应用完成、调理复位完成、批量完成。

---

## 9. 参数区（三区模型：运行区 / 编辑缓冲 / Retain）

| 参数块 | 地址 | Retain | 说明 |
|---|---|---|---|
| 调理参数 Stru_CondParams | D202–D458 | 快照锁存 | 三轴往复设置 + 全局参数 |
| 轴 Config（×3） | 各轴块内 Config 10 字 | 是 | 限位/初始位/容差/速度/加减速/脉冲当量/超时；另有 ConfigBuffer 编辑缓冲 |
| 电推杆 Config | D606–D659 | 是 | 见 §5 |
| 回零参数 | D887–D935（D886 为操作命令字） | 是 | 见下 |
| 系统参数 | 部分经 D481–D485 写入 | 是 | 力换算系数、超力阈值、软急停减速时间、分时上电间隔等 |
| 建零标志 | Retain | 是 | bZeroCommissioned |

### 9.1 调理参数（D202–D458）

- **每轴 Stru_ReciprocationSettings**：16 段目标位置 `arPosition[0..15]` + 16 段速度 `arVelocity[0..15]`（单位 度、度/秒）；统一加/减速度、起始参考位置；速度模式(0=各段独立/1=全段统一)、有效段数、本轴循环次数（可被全局覆盖）、信号模式(0=手动/1=每循环自动统一信号)、单程模式、本轴使能(FALSE=跳过该轴)、16 循环模式位。
- **全局参数（D445–D458）**：成角目标角度、尾板初始角度、完整流程批次数（老化）、批次间隔、总超时、HMI 等待确认超时、全局循环次数、全局信号模式。

### 9.2 回零参数（D887–D935）

脱限 Jog 速度；三轴各自的主 Jog 速度 / 微调速度 / 接近区距离 / 迟滞窗口 / Jog 加速度；扭矩防夹阈值；普通回零全流程超时、紧急回零单轴超时、脱限超时、MC_Stop 释放等待、接近区滑行等待、堵转判定窗口；回零执行模式(0 并行/1 顺序)、紧急回零最大重试次数、紧急回零允许电推杆校准开关。

### 9.3 参数操作命令（各块统一 ConfigCmd 字）

每个参数块（调理 / 三轴 Config / 电推杆 Config / 回零）统一一套操作：**加载**（运行区→缓冲）、**应用**（缓冲→运行区，需 FC_ValidateParams 校验通过）、**保存**（运行区→Retain）、**恢复工厂默认**、**加载默认值**；PLC 回写 bSuccess / bError / iParamErrorCode（应用失败时给出 iParamError + 轴 + 字段索引三元组定位，且运行区不被破坏）。**设备身份块（§10.2）例外**：其 Cmd 仅 Load / Apply 两个命令位（无 Save / ResetDefault），命令字为 `iErrorCode + bLoad/bApply/bSuccess/bError` 结构，与参数块 ConfigCmd（iErrorCode 在前）同构但位更少。

- 运行中参数锁定：AUTORUN 启动时锁存参数快照，运行中修改不生效。
- 工厂默认值由 PLC 内置（FC_Default*Config / FC_DefaultConditionParams）。

---

## 10. 设备身份区

两块结构体均已取得旧工程导出（`old/结构体/Stru_HMI_DeviceInfo_new.csv` / `Stru_HMI_DeviceIdentityWrite_new.csv`），字段、类型、打包以 CSV 为准。

### 10.1 设备信息只读镜像（Stru_HMI_DeviceInfo，D1000–D1004，PLC→HMI，5 字）

| 成员 | 类型 | 地址 | 内容 |
|---|---|---|---|
| diDeviceNoRaw | DINT | D1000–D1001 | 原始设备编号（工厂烧录 / 维护配置） |
| iMacWord0 | INT | D1002 | PLC MAC 地址字 0（= `_Ethernet.MACAddress[0]`） |
| iMacWord1 | INT | D1003 | PLC MAC 地址字 1（= `_Ethernet.MACAddress[1]`） |
| iMacWord2 | INT | D1004 | PLC MAC 地址字 2（= `_Ethernet.MACAddress[2]`） |

### 10.2 设备身份维护写入（Stru_HMI_DeviceIdentityWrite，D1010–D1013，双向，4 字）

| 成员 | 类型 | 地址 | 内容 |
|---|---|---|---|
| Cmd.iErrorCode | INT | D1010 | 错误反馈码（0=成功；身份非法 5005 / 锁定 5006） |
| Cmd.bLoad | BOOL | D1011.0 | 加载（运行区 → 编辑区） |
| Cmd.bApply | BOOL | D1011.1 | 应用（编辑区 diDeviceNoRaw → 运行区，需校验） |
| Cmd.bSuccess | BOOL | D1011.2 | PLC 回写：操作成功 |
| Cmd.bError | BOOL | D1011.3 | PLC 回写：操作失败 |
| Cmd.bPad0 / bPad1 | BOOL | D1011.4–.5 | 填充位 |
| diDeviceNoRaw | DINT | D1012–D1013 | 待写入的原始设备编号（DINT 双字对齐到偶字边界） |

- 身份块命令字仅 **Load / Apply**（无 Save / ResetDefault）；命令位为脉冲，R_TRIG 消费。
- Apply 校验失败回写 bError + iErrorCode（身份非法 5005 / 维护锁定 5006），不破坏已生效编号。

---

## 11. 硬接线 I/O 与总线（不占 D 区，仅备忘）

| 信号 | 类型 | 说明 |
|---|---|---|
| 物理急停 | I（常闭，有效=急停） | → HARD_ESTOP，断使能 |
| 复位按钮 | I | 硬件复位 |
| 脚踏开关 | I | 调理流程推进（与 HMI 软信号 D200.4 等效） |
| 电推杆原点传感器 | I（上行导通） | 含卡死 ON/OFF、与角度一致性校验 |
| Q_Pusher_Up / Q_Pusher_Down | Q | 电推杆正/反转（开关型，无调速），支持方向取反 |
| 3× M900 伺服（CiA402） | EtherCAT 总线 | MC_Power/Reset/Stop/Home/MoveAbsolute/MoveRelative/MoveJog/MoveBuffer |

---

## 12. 扫描分层架构（单写源硬约束）

```
CMD_PRECLEAR → INPUT_ADAPTER → MONITOR → ERROR_MANAGER → SAFETY_MANAGER
→ MODE_MANAGER → TASK_RUNNER → PARAM_SERVICE → POWER → OUTPUT_ADAPTER → HMI_OUTPUT
```

- 每个变量唯一写入者：`iState` 只归 MODE_MANAGER；Error 只归 ERROR_MANAGER；`bEnable` 只归 POWER；MC 指令只在 OUTPUT_ADAPTER 调用。
- 任务只写任务级 Cmd/Motion，通过 FaultReq 提交故障，不直写系统错误。
- 脉冲命令每扫 L0 清理；电平命令离态必须清残留。
- 分时上电：全轴上电由 PLC 按延时逐轴使能，bPoweringUp 过程位上报。

---

## 13. 待办 / 对齐检查清单

- [x] 寄存器布局权威来源已取得：旧工程变量表导出 `old/结构体/Stru_HMI_new.csv`（含 `Stru_HMI.csv` 旧版参照），等价替代 HMI 侧 `register-map.json`——新工程按 CSV 逐字段定义结构体并整体 `AT %MW200`，编译器自动打包对齐（BOOL 按位、REAL/DINT 双字），总长度 11856 bit = 741 字（D200–D940）已与 InoProShop 变量表截图核对一致。
- [x] 三轴块内偏移矛盾（状态区 D669–D717 vs 命令字 D702/D774/D846 偏移 33）：由 CSV 中三块同构结构体定义解决，块基址 669/741/813、步距 72 字不变。
- [ ] 联调首日用样例值确认 DINT/REAL 的 Modbus 传输字序（高/低字排列）与 HMI 一致（结构体内存布局已冻结，此项仅为传输层确认）。
- [ ] 确认电推杆编码器类型：相对（增量式，需校准流程）/ 绝对（可裁剪校准与自检 Step30–40）。
- [ ] 按本表预留 D200–D1012 窗口，契约区不得挪作他用。
- [ ] 待裁决：CSV 注释 `S1_StartProcess` 标注「启动流程 (电平)」，与本表 §3「S1 为脉冲消费」矛盾——HMI 实际按电平还是脉冲下发，需与 HMI 方确认（PLC 侧两种均可适配：电平用 R_TRIG 取上升沿同样安全）。
