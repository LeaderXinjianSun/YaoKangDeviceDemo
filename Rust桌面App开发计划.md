# Rust 桌面 App 渐进式开发计划

> 配套文档：[Rust桌面App技术方案.md](./Rust桌面App技术方案.md)
> 原则：**小步快跑、每阶段可运行可演示、先打通高风险项**。不一次性铺开全部功能，每阶段只做规定范围，完成并验证后再进入下一阶段。

## 总体节奏

| 阶段 | 主题 | 一句话目标 | 可否脱离真机 |
|---|---|---|---|
| P0 | 脚手架 + 参数库 | 深色壳能跑；rusqlite 建库与 `app_config` 表（默认 192.168.1.88:502） | 是 |
| P1 | 自动连接 + 参数页 + 按需只读 + D210 心跳/回读 | 开机按 SQLite 参数自动连接、断线经 D210 回读判定后自动重连（异步不卡 UI）；参数界面可改 IP/端口；进入调试页才读四轴坐标、离开即停；100ms 常驻写 D210 | 仿真器可 |
| P2 | 单轴点动 | 胸背轴"按1松0 / 取反 / 使能 / 停止"跑通 | 仿真器可 |
| P3 | 四轴调试 | 复用组件补齐臀腿/臀盘/电推杆，参数可下发 | 仿真器可 |
| P4 | 运动数组 | D2000 数组按钮触发上读 / 编辑 / 批量下发（不轮询） | 仿真器可 |
| P5 | SQLite 业务表 | 数组配方保存 / 载入 / 下发记录（`app_config` 参数表已在 P0 建好） | 是 |
| P6 | 诊断与安全 | 寄存器诊断页、断线清零、看门狗与按需读取联调 | 需真机 |
| P7 | 打包交付 | Windows 安装包与配置固化 | 需真机 |

> 通信总原则（第 8 条）：**程序不常驻轮询 PLC，只有当前界面按需读写对应寄存器，切换离开即停止读取。** 连接可保持长连接以降低延迟，但读任务由"界面订阅/按钮触发"驱动。**唯一例外：连接期间无论在哪个界面，后端每 100ms 常驻向 `D210`（随机数 INT，功能码 06）写一个新随机数作为上位机心跳，断开即停。**

> 关键路径是 **P1（地址映射 + REAL 字节序）**，这是技术方案中标注的最高风险项，故最早做；P2 验证最核心也最危险的"按1松0"。前面所有阶段都可用 Modbus 仿真器（ModbusSim / plc-ladder-sim）完成，P6 起必须接真机。

---

## P0 脚手架：能跑的空壳 + 参数库

**目标**：开发环境就绪，深色 + 左侧导航的空页面能启动；SQLite 与参数表就位并写入默认连接参数。

**任务**
- 用 `pnpm create tauri-app` 初始化 Tauri 2 + Vue 3 + TS + Vite。
- 接入 Naive UI（`n-config-provider` + `darkTheme`）、Vue Router、Pinia。
- 布局：`n-layout has-sider` 左侧 `n-menu`（占位：调试 / 运动数组 / 诊断 / 设置），右侧路由出口，底部状态栏放一个静态 PLC 灯。
- Rust 端引入 `rusqlite`（bundled），db 文件放 app data 目录；启动时建库、建 `app_config(key,value,updated_at)` 键值表，首启播种默认行：`plc.ip=192.168.1.88`、`plc.port=502`（以及 unit_id、字序、周期、心跳等默认值，见技术方案第 9 节）。
- 提供 `config_get_all / config_set(key,value)` 两个命令，设置页用一个最简表单验证读写与重启持久化。

**交付物**：`pnpm tauri dev` 启动，深色窗口、菜单可切换空页面；设置页能看到默认 IP/端口，改值保存后重启仍在（写入 SQLite 文件）。

**不做**：任何 Modbus、运动数组业务表、真实控件逻辑。

---

## P1 自动连接 + 按需只读 + D210 心跳（最高风险，优先）

**目标**：**开机按 SQLite 参数自动连接** PLC/仿真器（第 10 条）；连接后**仅在调试页**周期读 `D200~D207`，切到其它页/关闭页面立即停读；无论在哪个界面，连接期间都以 **100ms 间隔常驻向 `D210` 写随机数**（第 8 条心跳），并周期**回读 `D210` 判停**；掉线自动重连；**连接/读写/重连全程异步，UI 不卡死**；连接状态灯正确。

**任务**
- 后端建 `plc` 模块（单 crate 内分文件，结构从简）：
  - `address.rs`：`PlcMap { m_base, d_base }` 与 `d_reg/m_coil` 换算；
  - `codec.rs`：REAL 四种字序（ABCD/CDAB/BADC/DCBA）解码，配置项可选；
  - `client.rs`：`tokio-modbus 0.17` TCP 长连接；**连接监督器**（应用启动即从 `app_config` 取参数自动连接；TCP 报错或 **D210 回读连续失败**判离线；指数退避自动重连 1s→2s→5s→10s 封顶；手动断开置标志停止重连）；**订阅管理器**（group → 定时读任务，引用计数，退订即停）；**心跳任务**（连接成功即每 100ms 用功能码 06 写 `D210` 一个新 `u16` 随机数，独立 tokio 任务，断开即停）。
- socket 设连接/读写超时（3s/1s）；所有命令 `async`，连接、读写、退避重连全部在 tokio 任务内，前端只 await 事件，**任何情况下界面不卡死**；rusqlite 调用走 `spawn_blocking`。
- 所有报文（心跳写、D210 回读、订阅读、业务写）共用同一条连接与一把串行锁，心跳优先级最低、单次超时跳过不阻塞业务。
- 命令：`plc_connect(config)` / `plc_disconnect`（手动入口，开机自动连接不依赖前端点击）/ `plc_subscribe(group)` / `plc_unsubscribe(group)`；事件：`plc::status`（含 connecting/online/reconnecting/offline）、`plc::telemetry`。
- 前端 `api/plc.ts` + `stores/plc.ts`：**设置（参数）页做连接表单——IP（默认 192.168.1.88）/端口（502）/unit id/字序/基址，进入页面从 `app_config` 回填，保存写 SQLite；提供手动 连接/断开 按钮，已连接时改 IP/端口提示重连生效；展示自动重连状态**；调试页 `onMounted` 订阅 `telemetry`、`onUnmounted`/路由离开退订；监听事件更新坐标与状态灯。心跳与自动重连纯后端行为。
- 调试页右侧先做**只读坐标区**（胸背 mm，其余 °，3 位小数）。
- 本地用 Modbus 仿真器预置已知浮点值，核对地址与字序；用仿真器报文计数确认"离开页面后无读请求、但 D210 写仍以 100ms 持续"。

**验收**
- 进入调试页：改仿真器 `D200` 浮点，界面约 500ms 内正确刷新。
- 切到其它页或关闭页面：仿真器侧**不再收到读请求**；再切回恢复刷新。
- **连接成功后任意界面（含设置页）**：仿真器 `D210` 每约 100ms 收到一次写、且每次值不同；并能看到周期回读 `D210` 的报文。
- **开机自动连接**：仿真器已开时启动应用，不点任何按钮即自动连上、状态灯变绿。
- **断线自动重连**：关闭仿真器 → 状态灯变红/显示重连中（心跳停、读停）；重启仿真器 → 按退避自动重连成功，心跳与坐标刷新自动恢复；重连期间界面照常可切换、不卡死、不弹阻塞对话框。
- **不卡 UI**：PLC 地址不可达（如拔网线/IP 不通）时启动应用、反复切页，窗口动画与操作始终流畅；所有连接等待均带超时。
- 手动"断开"后不再自动重连；手动"连接"恢复；字序切换后数值正确。
- 参数页首屏显示默认 `192.168.1.88:502`；改为仿真器地址保存、重启应用后值仍在，并用该参数成功自动连机。

**不做**：任何业务写操作（点动/参数/数组）、运动数组业务表、非调试页的读取。

---

## P2 单轴点动：跑通"按1松0 / 取反"

**目标**：只做胸背轴一个轴，完整验证最核心的点动与安全机制。

**任务**
- 后端命令：`coil_set(addr,on)`、`coil_toggle(addr)`、`write_real(d,v)`；同一连接写操作串行化。
- **按1松0**：`AxisPad.vue` 上 `@mousedown/@touchstart` 发 ON，`@mouseup/@mouseleave/@touchend/@blur` 发 OFF。
- 后端**看门狗**：记录每个按下时刻，超时（默认 5s，可配）未收到释放则强制 OFF。
- 控件：调试使能(M200，保持)、Jog+(M201)、Jog-(M202)、取反、调试停止(M218)、普通停止(M101)。
- 断线 / 切页 / 窗口失焦时主动清掉所有置位点动线圈。

**验收（仿真器配合看线圈）**
- 按住按钮期间线圈为 1，松开立即为 0；拖出按钮、切窗、断线都能回 0。
- 长按超过看门狗阈值自动回 0；取反实现读—改—写且结果正确。
- 停止按钮可一键复位。

**不做**：Inc/Abs、其它三轴、参数批量、数组。

---

## P3 四轴调试：复用与参数下发

**目标**：把 P2 的 `AxisPad` 配成四份，补齐各轴能力差异与调试参数。

**任务**
- 用一份轴配置表驱动（基址、是否有使能、是否有 Inc、单位、参数 D 地址），不再复制页面：
  - 胸背 M200~M205 / D1000~D1004；臀腿 M206~M211 / D1006~D1010；
  - 臀盘 M212~M217 / D1012~D1016；电推杆仅 Jog±(M219/M220)+Abs(M221) / 目标 D1018。
- 调试速度 / 点动距离 / 目标位置输入，失焦或"应用"时 `write_real` 下发。
- Inc+ / Inc- / Go(Abs) 按钮：按 PLC 上升沿约定发**可配置宽度**短脉冲（P2 看门狗机制复用）。
- 左上轴切换菜单、右上运行(M110)/调试(M111)、退出调试(M120)、复位(M102) 接好。
- 页面基本按参考图布局，样式从简。

**验收**：四个轴切换时控件按能力正确裁剪；参数写入后仿真器对应 D 地址值正确；各命令线圈地址与 xlsx 一致。

**不做**：运动数组、SQLite、复杂美化。

---

## P4 运动数组：D2000 上读 / 编辑 / 下发

**目标**：16 步运动表格可在 PLC 与界面间双向流转；**全部为按钮触发的一次性读写，进入页面不自动读、不订阅、不轮询**（第 8 条）。

**任务**
- `arrays.rs`：用"段起始 + 步号×2"公式表达 13 个段（坐标/速度/加减速/间隔），集中各段限值与 REAL/DINT 解码。
- 命令：`array_upload(depth,mode,data)`（功能码 16 按段批量写，最后写 D1100/D1101）、`array_download()`；二者均为**调用一次执行一次**，不挂任何定时任务。
- 前端 `n-data-table` 可编辑表格：深度 1~16 动态增减行，模式下拉（调理/治疗/老化），超限单元格标红并阻止下发。
- 操作按钮：从 PLC 上读、整组下发；下发后可再点"上读"做差异对比。进入/离开本页均不产生 PLC 报文，用仿真器报文计数确认。
- DINT 运动间隔按配置字序编解码。

**验收**：进入运动数组页仿真器无读请求；点"上读"后仿真器数据与表格一致；编辑后下发，回读与界面一致；超限时禁止下发并有提示；切走页面期间同样无任何报文。

**不做**：本地持久化（下一阶段）、任何自动/定时刷新。

---

## P5 SQLite 业务表：配方与下发记录

**目标**：数组可存本地、随时载入，下发有记录。（rusqlite 与 `app_config` 参数表已在 P0 引入，本阶段只加业务表）

**任务**
- 在现有库中建 `move_array / move_step / download_log`（见技术方案第 6 节），连接参数仍由 `app_config` 管理，两处不重复造配置。
- 命令：配方增删改查、克隆、按 id 载入；每次下发写一条 `download_log`。
- 数组页加"保存为配方 / 载入配方 / 配方列表"；Demo 先不做导入导出 JSON（可留到后续）。

**验收**：重启应用后配方仍在；载入配方能回填表格并成功下发；下发记录可查。

**不做**：多用户、云同步、JSON 导入导出（非 Demo 必需）。

---

## P6 诊断与安全联调（接真机）

**目标**：真机核对地址/字序/触发方式，补齐安全兜底。

**任务**
- 寄存器诊断页：任意 M/D 原始值读取、单线圈/单寄存器强制写、报文与错误日志（借 ModbusSim 的日志思路）。
- 真机核对：M/D 的 Modbus 实际偏移、REAL/DINT 字序、CMD 是脉冲还是电平，结果固化进默认配置。
- 安全复核：急停/安全继电器信号若可读则上送并强提醒；断线/失焦/切页清零全场景回归；看门狗阈值与脉冲宽度现场整定。
- **按需读取真机回归（第 8 条）**：用抓包/PLC 连接统计确认——只有调试页打开时有 `D200~D207` 周期读；运动数组页只在点按钮时各发一次读写；诊断页仅手动刷新；设置等其它页面零报文；断开连接后所有订阅清空、无残留读任务。
- **D210 心跳真机回归**：连接后切遍所有页面，确认 `D210` 始终以约 100ms 收到随机数写；与点动/数组下发同时操作时无报文错乱、业务写不被心跳阻塞；确认 PLC 侧超时判停逻辑与 100ms 间隔匹配（必要时整定超时倍数）。
- **断线判定/自动重连真机回归（第 10 条）**：分别验证拔网线、PLC 断电、关交换机三种掉线，确认 D210 回读/超时能在预期时间内判停并进入重连，恢复后自动回连且订阅与心跳自愈；长时间掉线时退避封顶不刷报文风暴；掉线及重连全程界面操作流畅不卡死；手动断开不重连。
- 必要时读取 IO（X/Y 映射）做状态展示（同样仅在对应诊断界面短时读取）。

**验收**：真机上四轴点动、定位、整组数组下发均正确；异常场景下不会出现持续运动或误动作；非当前界面对应寄存器无任何后台读写。

---

## P7 打包交付

**目标**：产出可在普通工厂 PC 安装运行的 Windows 程序。

**任务**
- `tauri.conf.json` 固化：窗口标题/图标、WebView2 引导；**默认连接参数（192.168.1.88:502 等）由首启播种进 SQLite `app_config`，不写死在代码/JSON 里**。
- `pnpm tauri build` 生成 MSI/NSIS 安装包，在干净 Windows 机器验证安装与运行。
- 整理一份现场可改的配置项说明（参数界面各字段：IP、端口、unit id、字序、基址、看门狗/脉冲时长、心跳间隔与 D210 地址、限值；均存 SQLite）。

**验收**：无开发环境的 PC 上安装后能连机完成调试与数组下发全流程。

---

## 阶段依赖与建议

- 主线顺序：`P0 → P1 → P2 → P3 → P4 → P5`，**P1/P2 是关键**，地址字序与点动安全不过不往后走。
- P5（SQLite 业务表）与 P3/P4 解耦，人力允许时可并行；连接参数表 `app_config` 已在 P0 落地，业务表结构按技术方案第 6 节先定。
- 每个阶段完成的标准统一为：**在仿真器（或真机）上演示验收项 + 代码可直接运行**，不堆半成品。
- 始终遵守 Demo 约束：单 crate、模块分文件、样式从简；只有当协议逻辑需要独立测试时，才按技术方案 12.2 拆 `plc-core` workspace，Demo 阶段不提前拆。
